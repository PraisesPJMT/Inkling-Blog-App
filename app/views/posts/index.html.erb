<h2><%= @user.name %>'s Posts</h2>
<div class="user-list">
    <% @user.posts.each do |post| %>
        <a id="post-box<%=post.id%>" class="user-post-box" href=<%= user_post_path(@user.id, post.id) %>>
            <h4><%= post.title %></h4>
            <p><%= post.text[0..150] %>...</p>
            <div class="user-post-count">
                <span>Comments: <%= post.comments_counter %></span>
                <span class="seperator">|</span>
                <span>Likes: <%= post.likes_counter %></span>
            </div>
            <div class="comment-box">
                <% post.comments.includes(:author).each do |comment| %>
                    <div class="comment">
                        <img class="comment-avatar" src=<%= comment.author.photo %> alt=<%= comment.author.name %>>
                        <div class="comment-card">
                            <p class="comment-details"><span><%= comment.author.name %></span> | <span><%= comment.created_at %></span></p>
                            <p><%= comment.text %></p>
                        </div>
                    </div>
                <% end %>
            </div>
        </a>
    <% end %>
</div>
